{
  "name": "create-repository-check-operation-of-dispatch-events",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "openapi": "npx @redocly/cli bundle ./openapi/specification.yaml --output ./openapi/specification.bundled.yaml"
  }
}
